[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pretty-interface-thing"
version = "0.0.0"
description = "Awesome interface library for pygame. Coming soon!"
authors = [{name = "Imoze", email = "game-f-90@mail.ru"}]